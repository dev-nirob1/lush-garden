import SectionTitle from '../shared/SectionTitle';

const Blog = () => {
    return (
        <div>
            <SectionTitle title="Interesting blog to read" />
        </div>
    );
};

export default Blog;